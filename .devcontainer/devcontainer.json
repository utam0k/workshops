{    
    "build": { "dockerfile": "Dockerfile" },
	"features": {    
		"docker-in-docker": {
			"version": "latest",
			"moby": true,
			"dockerDashComposeVersion": "v2"
		}
	},
	"postStartCommand": "sudo chown ubuntu:ubuntu /tmp"
}
